'use strict';

describe('myApp.accidentes module', function() {

  beforeEach(module('myApp.accidentes'));

  describe('accidentes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var accidentesCtrl = $controller('accidentesCtrl');
      expect(accidentesCtrl).toBeDefined();
    }));

  });
});