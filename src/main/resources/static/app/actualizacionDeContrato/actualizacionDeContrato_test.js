'use strict';

describe('myApp.actualizacionDeContrato module', function() {

  beforeEach(module('myApp.actualizacionDeContrato'));

  describe('actualizacionDeContrato controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actualizacionDeContratoCtrl = $controller('actualizacionDeContratoCtrl');
      expect(actualizacionDeContratoCtrl).toBeDefined();
    }));

  });
});