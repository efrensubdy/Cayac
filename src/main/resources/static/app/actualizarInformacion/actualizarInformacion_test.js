'use strict';

describe('myApp.actualizarInformacion module', function() {

  beforeEach(module('myApp.actualizarInformacion'));

  describe('actualizarInformacion controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actualizarInformacionCtrl = $controller('actualizarInformacionCtrl');
      expect(actualizarInformacionCtrl).toBeDefined();
    }));

  });
});