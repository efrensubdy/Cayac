'use strict';

describe('myApp.actualizarServicios module', function() {

  beforeEach(module('myApp.actualizarServicios'));

  describe('actualizarServicios controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actualizarServiciosCtrl = $controller('actualizarServiciosCtrl');
      expect(actualizarServiciosCtrl).toBeDefined();
    }));

  });
});