'use strict';

describe('myApp.agregarActividades module', function() {

  beforeEach(module('myApp.agregarActividades'));

  describe('agregarActividades controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var agregarActividadesCtrl = $controller('agregarActividadesCtrl');
      expect(agregarActividadesCtrl).toBeDefined();
    }));

  });
});