'use strict';

describe('myApp.analisisDeActividades module', function() {

  beforeEach(module('myApp.analisisDeActividades'));

  describe('analisisDeActividades controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var analisisDeActividadesCtrl = $controller('analisisDeActividadesCtrl');
      expect(analisisDeActividadesCtrl).toBeDefined();
    }));

  });
});