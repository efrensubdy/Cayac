'use strict';

describe('myApp.auditoria2.0 module', function() {

  beforeEach(module('myApp.auditoria2.0'));

  describe('auditoria2.0 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var auditoria2.0Ctrl = $controller('auditoria2.0Ctrl');
      expect(auditoria2.0Ctrl).toBeDefined();
    }));

  });
});