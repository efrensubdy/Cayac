'use strict';

describe('myApp.auditoria module', function() {

  beforeEach(module('myApp.auditoria'));

  describe('auditoria controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var auditoriaCtrl = $controller('auditoriaCtrl');
      expect(auditoriaCtrl).toBeDefined();
    }));

  });
});