'use strict';

describe('myApp.diagnostico module', function() {

  beforeEach(module('myApp.diagnostico'));

  describe('diagnostico controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var diagnosticoCtrl = $controller('diagnosticoCtrl');
      expect(diagnosticoCtrl).toBeDefined();
    }));

  });
});