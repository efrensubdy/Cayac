'use strict';

describe('myApp.ejecucionCNCE module', function() {

  beforeEach(module('myApp.ejecucionCNCE'));

  describe('ejecucionCNCE controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ejecucionCNCECtrl = $controller('ejecucionCNCECtrl');
      expect(ejecucionCNCECtrl).toBeDefined();
    }));

  });
});