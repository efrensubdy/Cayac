'use strict';

describe('myApp.estandaresMinimos module', function() {

  beforeEach(module('myApp.estandaresMinimos'));

  describe('estandaresMinimos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var estandaresMinimosCtrl = $controller('estandaresMinimosCtrl');
      expect(estandaresMinimosCtrl).toBeDefined();
    }));

  });
});