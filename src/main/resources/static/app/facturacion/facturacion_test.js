'use strict';

describe('myApp.facturacion module', function() {

  beforeEach(module('myApp.facturacion'));

  describe('facturacion controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var facturacionCtrl = $controller('facturacionCtrl');
      expect(facturacionCtrl).toBeDefined();
    }));

  });
});