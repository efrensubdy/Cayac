'use strict';

describe('myApp.gestionAuditoria module', function() {

  beforeEach(module('myApp.gestionAuditoria'));

  describe('gestionAuditoria controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gestionAuditoriaCtrl = $controller('gestionAuditoriaCtrl');
      expect(gestionAuditoriaCtrl).toBeDefined();
    }));

  });
});