'use strict';

describe('myApp.gestionDeAccidentes module', function() {

  beforeEach(module('myApp.gestionDeAccidentes'));

  describe('gestionDeAccidentes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gestionDeAccidentesCtrl = $controller('gestionDeAccidentesCtrl');
      expect(gestionDeAccidentesCtrl).toBeDefined();
    }));

  });
});