'use strict';

describe('myApp.gestionDeActividades module', function() {

  beforeEach(module('myApp.gestionDeActividades'));

  describe('gestionDeActividades controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gestionDeActividadesCtrl = $controller('gestionDeActividadesCtrl');
      expect(gestionDeActividadesCtrl).toBeDefined();
    }));

  });
});