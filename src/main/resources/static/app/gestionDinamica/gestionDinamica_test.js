'use strict';

describe('myApp.gestionDinamica module', function() {

  beforeEach(module('myApp.gestionDinamica'));

  describe('gestionDinamicaCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gestionDinamicaCtrl = $controller('gestionDinamicaCtrl');
      expect(gestionDinamicaCtrl).toBeDefined();
    }));

  });
});