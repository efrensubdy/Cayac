'use strict';

describe('myApp.indicadores module', function() {

  beforeEach(module('myApp.indicadores'));

  describe('indicadores controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var indicadoresCtrl = $controller('indicadoresCtrl');
      expect(indicadoresCtrl).toBeDefined();
    }));

  });
});