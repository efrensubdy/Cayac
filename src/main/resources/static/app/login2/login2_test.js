'use strict';

describe('myApp.login2 module', function() {

  beforeEach(module('myApp.login2'));

  describe('login2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var login2Ctrl = $controller('login2Ctrl');
      expect(login2Ctrl).toBeDefined();
    }));

  });
});