'use strict';

describe('myApp.login3 module', function() {

  beforeEach(module('myApp.login3'));

  describe('login3 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var login2Ctrl = $controller('login3Ctrl');
      expect(login3Ctrl).toBeDefined();
    }));

  });
});