'use strict';

describe('myApp.mensajeria module', function() {

  beforeEach(module('myApp.mensajeria'));

  describe('mensajeria controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mensajeriaCtrl = $controller('mensajeriaCtrl');
      expect(mensajeriaCtrl).toBeDefined();
    }));

  });
});