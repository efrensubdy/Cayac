'use strict';

describe('myApp.notificaciones module', function() {

  beforeEach(module('myApp.notificaciones'));

  describe('notificaciones controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var notificacionesCtrl = $controller('notificacionesCtrl');
      expect(notificacionesCtrl).toBeDefined();
    }));

  });
});