'use strict';

describe('myApp.planDeTrabajo module', function() {

  beforeEach(module('myApp.planDeTrabajo'));

  describe('planDeTrabajo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var planDeTrabajoCtrl = $controller('planDeTrabajoCtrl');
      expect(planDeTrabajoCtrl).toBeDefined();
    }));

  });
});