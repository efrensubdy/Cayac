'use strict';

describe('myApp.registroDeAuditoria module', function() {

  beforeEach(module('myApp.registroDeAuditoria'));

  describe('registroDeAuditoria controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registroDeAuditoriaCtrl = $controller('registroDeAuditoriaCtrl');
      expect(registroDeAuditoriaCtrl).toBeDefined();
    }));

  });
});