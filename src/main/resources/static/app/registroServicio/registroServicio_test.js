'use strict';

describe('myApp.registroServicio module', function() {

  beforeEach(module('myApp.registroServicio'));

  describe('registroServicio controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registroServicioCtrl = $controller('registroServicioCtrl');
      expect(registroServicioCtrl).toBeDefined();
    }));

  });
});