'use strict';

describe('myApp.revisionDeDiagnostico module', function() {

  beforeEach(module('myApp.revisionDeDiagnostico'));

  describe('revisionDeDiagnostico controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var revisionDeDiagnosticoCtrl = $controller('revisionDeDiagnostico1Ctrl');
      expect(revisionDeDiagnosticoCtrl).toBeDefined();
    }));

  });
});