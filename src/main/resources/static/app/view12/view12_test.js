'use strict';

describe('myApp.view12 module', function() {

  beforeEach(module('myApp.view12'));

  describe('view12 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view12Ctrl = $controller('View12Ctrl');
      expect(view12Ctrl).toBeDefined();
    }));

  });
});