'use strict';

describe('myApp.view13 module', function() {

  beforeEach(module('myApp.view13'));

  describe('view13 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view13Ctrl = $controller('View13Ctrl');
      expect(view13Ctrl).toBeDefined();
    }));

  });
});